services:
  forgepilot:
    image: ${FORGEPILOT_IMAGE:-ghcr.io/stejas7/forgepilot-ai:latest}
    container_name: forgepilot-ai
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-prod}
